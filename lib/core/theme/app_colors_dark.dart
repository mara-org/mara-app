import 'package:flutter/material.dart';

/// Dark mode color definitions for the app.
class AppColorsDark {
  AppColorsDark._();

  // Primary colors (same as light, but can be adjusted)
  static const Color primary = Color(0xFFFFFFFF);
  static const Color secondary = Color(0xFF000000);

  // Background colors for dark mode
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color error = Color(0xFFCF6679);

  // Gradient colors (same, but can be dimmed)
  static const Color gradientStart = Color(0xFF0EA5C6);
  static const Color gradientEnd = Color(0xFF10A9CC);

  // Language selector card color (darker)
  static const Color languageCardBackground = Color(0xFF1E3A40);

  // Language button color (same)
  static const Color languageButtonColor = Color(0xFF0EA5C6);

  // Onboarding gradient colors (darker)
  static const Color onboardingGradientStart = Color(0xFF1A2B2F);
  static const Color onboardingGradientEnd = Color(0xFF1E1E1E);
  static const Color personalizedGradientStart = Color(0xFF0D4A5C);
  static const Color personalizedGradientEnd = Color(0xFF0A3744);

  // Text colors
  static const Color textPrimary = Color(0xFFE5E5E5);
  static const Color textSecondary = Color(0xFF9CA3AF);

  // Background colors
  static const Color backgroundLight = Color(0xFF1E1E1E);
  static const Color borderColor = Color(0xFF2D2D2D);

  // Button colors
  static const Color appleButtonColor = Color(0xFFFFFFFF);

  // Gender button colors (darker)
  static const Color femaleButtonColor = Color(0xFF4A2C2C);

  // Permission screen colors (darker)
  static const Color cameraPermissionBackground = Color(0xFF2A3F4F);
  static const Color permissionEnabled = Color(0xFF10B981);
  static const Color permissionDisabled = Color(0xFF4A5B60);
  static const Color permissionCardBackground = Color(0xFF1A3A40);

  // Home screen colors
  static const Color homeHeaderBackground = Color(0xFF0EA5C6);
  static const Color homeCardBackground = Color(0xFF2A2A2A);
  static const Color homeVitalSignsBackground = Color(0xFF0EA5C6);
  static const Color homeTipBackground = Color(0xFF2A2A2A);

  // Card background for dark mode
  static const Color cardBackground = Color(0xFF2A2A2A);

  // Accent color
  static const Color accent = Color(0xFF0EA5C6);

  // Water color for charts
  static const Color waterColor = Color(0xFF0EA5C6);
}
