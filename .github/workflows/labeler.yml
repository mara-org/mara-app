# Mara Automation – Auto Labeler
# Automatically labels PRs based on file paths
# Helps organize and categorize pull requests

name: Mara Automation – Auto Labeler

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  label:
    name: Auto Label PR
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Label PR by paths
        run: |
          echo "⚠️ Labeler configuration removed - manual labeling required"
          echo "PR can be labeled manually based on changed files"
          # Labeler temporarily disabled due to configuration issues

